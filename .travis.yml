language: python
python:
  - "3.7"

os:
  - linux
  - osx

install:
    - pip install flake8
    - pip install pytest
    - pip install pytest-cov
    - pip install codecov
    - pip install -r requirements.txt

script:
    - make lint
    - make test
    - make coverage


after_success:
    - codecov
